<div>
                   Ingredients:
                   <ul>
                       {item.ingredients.map((ingredient) => <li key={ingredient.id}>{ingredient.name}</li>)}
                    </ul>
               </div>